{
  "name": "dotfiles",
  "version": "0.2.0",
  "private": true,
  "description": "Personal dotfiles and skills maintained with an agent",
  "bin": {
    "dot": "./dot"
  },
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dot.ts",
    "build": "bun build scripts/dot.ts --compile --outfile dot",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude '**/*.e2e.test.ts'",
    "test:e2e": "bun run build && vitest run tests/dot.e2e.test.ts",
    "lint": "oxlint --deny-warnings scripts tests",
    "format": "oxfmt --write scripts tests package.json tsconfig.json vitest.config.ts",
    "format:check": "oxfmt --check scripts tests package.json tsconfig.json vitest.config.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.7",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
