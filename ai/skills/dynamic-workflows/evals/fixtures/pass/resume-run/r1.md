First read the manifest and progress. Reuse the completed auth-scan and billing-scan outputs; do not rerun completed work. Run only the missing docs-scan. After docs-scan finishes, run adversarial-review because it depends on all scans.

