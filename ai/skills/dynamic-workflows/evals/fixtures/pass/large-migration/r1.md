I would ask for approval before file edits. The workflow has a concurrency cap of 6.

1. Phase: discover SDK v1 usage.
2. Phase: split packages into non-overlapping write scopes.
3. Phase: implement by package group.
4. Phase: verify with typecheck, lint, and tests.

