I would try tool discovery with tool_search first. If no subagent tools are available, I cannot fan out to parallel subagents in this session. I would state that limitation and use a sequential fallback, inspecting the modules one at a time.

