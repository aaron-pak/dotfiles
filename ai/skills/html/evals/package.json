{
  "name": "html-skill-evals",
  "private": true,
  "version": "1.0.0",
  "description": "Browser-driven regression evals for the /html skill",
  "type": "commonjs",
  "scripts": {
    "verify": "node run.js",
    "smoke": "node run.js --smoke",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
