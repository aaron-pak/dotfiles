Launch brief for `tiny-audit`.

Manifest:

1. Discover: run the `src/auth.ts` and `src/billing.ts` agents in parallel, each returning the requested structured findings.
2. Verify: after both Discover agents finish, run the `adversary` agent to try to refute the findings.

This is a Codex execution plan, not direct JavaScript execution. I would translate the workflow, not execute the JavaScript. I would present the manifest before running.
