I would require approval before any push or deployment. Split the workflow into phase boundaries: audit, propose fixes, request sign-off, apply fixes, request approval for PR creation, and require a separate deploy sign-off. Because workflows have no mid-run user input, sign-off between stages should be separate. I would not automatically deploy.

